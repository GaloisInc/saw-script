// 2025-ish version of saw: respecting monadic type at top-level
print "starting";

enable_experimental;
load_sawcore_from_file "EnumProto.sawcore";

let printNorm m e =
  print (show_term (normalize_term (parse_core_mod m e)));

let printNorm_e2 = printNorm "EnumProto";

printNorm_e2 "test1";

print "done";
