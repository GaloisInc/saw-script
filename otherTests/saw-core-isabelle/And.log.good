Loading file "And.saw"
Loading file "saw/isa_test.saw"
Successfully translated theory And


Translation successful. Generating theory files...


Generating And.thy
Successfully wrote '../isabelle/And.thy'
Wrote Isabelle theory files to: ../isabelle
theory "And"
imports "Cryptol.Cryptol"
begin

context includes cryptol_translation_syntax begin
cryptol_definition and_True_False :: "Bit" where
"and_True_False  \<equiv> (True &&`{Bit} False) ==`{Bit} False"

cryptol_definition and_True_True :: "Bit" where
"and_True_True  \<equiv> (True &&`{Bit} True) ==`{Bit} True"

cryptol_definition and_logic :: "{'a} ((Logic 'a) =?> ('a \<Rightarrow> ('a \<Rightarrow> 'a)))" where
"and_logic x y \<equiv> x &&`{'a} y"

cryptol_definition and_valid :: "{'a} ((Logic 'a,Eq 'a) =?> ('a \<Rightarrow> ('a \<Rightarrow> Bit)))" where
"and_valid x y \<equiv> (and_logic`{'a} x y) ==`{'a} (x &&`{'a} y)"

cryptol_definition and_valid_Bit :: "Bit \<Rightarrow> (Bit \<Rightarrow> Bit)" where
"and_valid_Bit  \<equiv> and_valid`{Bit}"

cryptol_definition and_valid_seq :: "{'a,'b} ((fin 'a,Logic 'b,Eq 'b) =?> ((['a]'b) \<Rightarrow> ((['a]'b) \<Rightarrow> Bit)))" where
"and_valid_seq  \<equiv> and_valid`{['a]'b}"

cryptol_definition and_valid_seq_seq_word :: "([32][16][8]) \<Rightarrow> (([32][16][8]) \<Rightarrow> Bit)" where
"and_valid_seq_seq_word  \<equiv> and_valid_seq`{32,[16][8]}"

cryptol_definition and_valid_word :: "{'a} ((fin 'a) =?> ((['a]) \<Rightarrow> ((['a]) \<Rightarrow> Bit)))" where
"and_valid_word  \<equiv> and_valid`{['a]}"

cryptol_definition and_word_lit :: "Bit" where
"and_word_lit  \<equiv> ((list_to_seq [True,True,False,True] :: [4]) &&`{[4]} (list_to_seq [False,True,True,True] :: [4])) ==`{[4]} (list_to_seq [False,True,False,True] :: [4])"

end
end

