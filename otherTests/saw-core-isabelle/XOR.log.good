Loading file "XOR.saw"
Loading file "saw/isa_test.saw"
Successfully translated theory XOR
Translation successful. Generating theory files...
Generating XOR.thy
Successfully wrote '../isabelle/XOR.thy'
theory "XOR"
imports "Cryptol.Cryptol"
begin

context includes cryptol_translation_syntax begin
cryptol_definition xor_logic :: "{'a} ((Logic 'a,Eq 'a) =?> ('a \<Rightarrow> ('a \<Rightarrow> 'a)))" where
"xor_logic x y \<equiv> x ^`{'a} y"

cryptol_definition xor_truth_table :: "Bit" where
"xor_truth_table  \<equiv> ((False ^`{Bit} False) ==`{Bit} False) &&`{Bit} (((True ^`{Bit} False) ==`{Bit} True) &&`{Bit} (((False ^`{Bit} True) ==`{Bit} True) &&`{Bit} ((True ^`{Bit} True) ==`{Bit} False)))"

cryptol_definition xor_valid :: "{'a} ((Logic 'a,Eq 'a) =?> ('a \<Rightarrow> ('a \<Rightarrow> Bit)))" where
"xor_valid x y \<equiv> (xor_logic`{'a} x y) ==`{'a} (x ^`{'a} y)"

cryptol_definition xor_valid_Bit :: "Bit \<Rightarrow> (Bit \<Rightarrow> Bit)" where
"xor_valid_Bit  \<equiv> xor_valid`{Bit}"

cryptol_definition xor_valid_seq :: "{'a,'b} ((fin 'a,Logic 'b,Eq 'b) =?> ((['a]'b) \<Rightarrow> ((['a]'b) \<Rightarrow> Bit)))" where
"xor_valid_seq  \<equiv> xor_valid`{['a]'b}"

cryptol_definition xor_valid_seq_seq_word :: "([32][16][8]) \<Rightarrow> (([32][16][8]) \<Rightarrow> Bit)" where
"xor_valid_seq_seq_word  \<equiv> xor_valid_seq`{32,[16][8]}"

cryptol_definition xor_valid_word :: "{'a} ((fin 'a) =?> ((['a]) \<Rightarrow> ((['a]) \<Rightarrow> Bit)))" where
"xor_valid_word  \<equiv> xor_valid`{['a]}"

cryptol_definition xor_word_lit :: "Bit" where
"xor_word_lit  \<equiv> ((list_to_seq [True,True,False,True] :: [4]) ^`{[4]} (list_to_seq [False,True,False,True] :: [4])) ==`{[4]} (list_to_seq [True,False,False,False] :: [4])"

end
end

