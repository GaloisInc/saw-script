// needs to be loaded ad-hoc until enum support is added to the Cryptol importer
enable_experimental;

let nm = "Enums";
cmod <- str_concats ["./cryptol/",nm,".cry"];
write_isabelle_cryptol_modules [] [cmod] "./isabelle";
thy <- exec "cat" [str_concats ["./isabelle/",nm,".thy"]] "";
print thy;