enable_experimental;

// Obligation-shape litmus: raw function-result error must become an explicit
// unreachability obligation rather than a fabricated function.
let probe = parse_core "error (Nat -> Nat) \"boom\"";

write_lean_term "RawErrorFunctionProbe" [] [] "emitted.lean" probe;
