enable_experimental;

// Obligation-shape litmus: index-position fix must use the raw generic
// unique-fixed-point contract, not wrapped value-domain fix helpers.
let probe = parse_core "Vec (fix Nat (\\(n : Nat) -> n)) Bool";

write_lean_term "FixRawIndexProbe" [] [] "emitted.lean" probe;
