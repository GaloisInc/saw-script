enable_experimental;

// Obligation-shape litmus: raw Nat/index-position error must become an explicit
// unreachability obligation rather than a default Nat.
let probe = parse_core "error Nat \"boom\"";

write_lean_term "RawErrorNatProbe" [] [] "emitted.lean" probe;
