enable_experimental;

// Obligation-shape litmus: Cryptol signed modulus by zero must expose a
// nonzero divisor contract or checked SAW-exact total semantics.
let probe =
  parse_core "ecSMod (TCNum 8) 0xf9 0x00";

write_lean_term "CryptolEcSModZeroProbe" [] [] "emitted.lean" probe;
