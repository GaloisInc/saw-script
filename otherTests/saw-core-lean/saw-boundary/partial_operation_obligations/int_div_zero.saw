enable_experimental;

// Boundary litmus: integer division by zero must be proof-carrying.
let probe = parse_core "intDiv (natToInt 1) (natToInt 0)";

write_lean_term "IntDivZeroProbe" [] [] "-" probe;
