enable_experimental;

// Boundary litmus: zero divisors are partial SAWCore surfaces. The Lean
// backend should expose a nonzero-divisor proof obligation, not silently choose
// a total value.
let probe = parse_core "divNat 1 0";

write_lean_term "NatDivZeroProbe" [] [] "-" probe;
