enable_experimental;

// Boundary litmus: a partial operation used as a function value cannot expose
// its nonzero-divisor obligation until the divisor argument is present. Reject
// this shape rather than emitting an unchecked total-looking Lean function.
let probe = parse_core "divNat 1";

write_lean_term "UnderAppliedPartialProbe" [] [] "-" probe;
