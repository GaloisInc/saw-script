Generated-index evidence now discharges direct `i < n` bounds, but reverse still exposes derived bounds over transformed indices that the current Lean proof outline does not discharge. True differential tests may not rely on `sorry`.
