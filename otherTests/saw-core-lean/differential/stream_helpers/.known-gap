Finite stream-helper observations still expose a Stream.rec / stream-construction convention gap: wrapped MkStream/helper results flow into raw Stream.rec positions, and fixing this requires a separate stream/recursor design rather than a fixture-specific wrapper patch.
