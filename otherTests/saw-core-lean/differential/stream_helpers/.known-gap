Finite stream-helper observations currently require proof-carrying stream
productivity/totality evidence before they can serve as executable
differential parity tests. The emitted artifact now elaborates only by using
proof stubs, and the differential harness correctly rejects relying on those
stubs.
