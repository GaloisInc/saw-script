// TRUE differential litmus for finite vector literals and indexing.

let observed = {{ (([1, 2, 3] : [3][8]) @ 1) == 2 }};

print (str_concat "SAW_OBSERVED: " (show (eval_bool observed)));

write_lean_term
  "Observed" [] []
  "observed.lean"
  observed;
