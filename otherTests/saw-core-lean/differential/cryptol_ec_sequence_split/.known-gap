Generated-index evidence now discharges direct `i < n` bounds, but split still exposes derived bounds over offsets and subtracted indices that the current Lean proof outline does not discharge. True differential tests may not rely on `sorry`.
