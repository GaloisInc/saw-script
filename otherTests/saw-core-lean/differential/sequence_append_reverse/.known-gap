Generated-index evidence now discharges direct `i < n` bounds, but append/reverse still expose derived bounds over offsets and transformed indices that the current Lean proof outline does not discharge. True differential tests may not rely on `sorry`.
