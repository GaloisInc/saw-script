Direct Cryptol.sawcore `ecSDiv` and `ecSMod` currently leave residual
`Prelude.Nat__rec` in the emitted term, which the Lean translator deliberately
rejects until there is a checked recursor mapping or proof-carrying lowering.
