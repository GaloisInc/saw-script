/- Mandatory imports from saw-core-lean -/
import CryptolToLean

open CryptolToLean.SAWCorePrimitives

/- Code generated by saw-core-lean -/
namespace StreamFibs
  noncomputable def streamFibs (x : CryptolToLean.SAWCoreVectors.Vec 8
    Bool) : CryptolToLean.SAWCoreVectors.Vec 32 Bool :=
    @Stream.rec (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (bvToNat 8 x)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => x') (mkStreamFixPair
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (CryptolToLean.SAWCoreVectors.Vec
    32 Bool) (error (CryptolToLean.SAWCoreVectors.Vec 32 Bool)
    "fix lookup out of bounds") (error (CryptolToLean.SAWCoreVectors.Vec 32
    Bool) "fix lookup out of bounds") (fun lkA_ lkB_ i_ => streamIdx
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (pairFst (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) ((fun (x' : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => @PairType.PairValue (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (@Stream.MkStream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (i : Nat) => atWithDefault 1
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (@Stream.rec
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => y) x')) #v[bvNat 32 0] i))
    (@Stream.MkStream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)
    (fun (i : Nat) => atWithDefault 1 (CryptolToLean.SAWCoreVectors.Vec 32 Bool)
    (bvAdd 32 (Pair_fst (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (PairType
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType) (@PairType.PairValue
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (PairType
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType) (@Stream.rec
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => x'') x'))
    (@PairType.PairValue (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType
    (@Stream.rec (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => y) x')) UnitType.Unit)))
    (Pair_fst (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType (Pair_snd
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (PairType
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType) (@PairType.PairValue
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (PairType
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType) (@Stream.rec
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => x'') x'))
    (@PairType.PairValue (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType
    (@Stream.rec (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => y) x')) UnitType.Unit)))))
    #v[bvNat 32 1] i))) (PairType.PairValue (Stream.MkStream lkA_)
    (Stream.MkStream lkB_)))) i_) (fun lkA_ lkB_ i_ => streamIdx
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (pairSnd (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) ((fun (x' : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => @PairType.PairValue (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (@Stream.MkStream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (i : Nat) => atWithDefault 1
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (@Stream.rec
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => y) x')) #v[bvNat 32 0] i))
    (@Stream.MkStream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)
    (fun (i : Nat) => atWithDefault 1 (CryptolToLean.SAWCoreVectors.Vec 32 Bool)
    (bvAdd 32 (Pair_fst (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (PairType
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType) (@PairType.PairValue
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (PairType
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType) (@Stream.rec
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => x'') x'))
    (@PairType.PairValue (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType
    (@Stream.rec (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => y) x')) UnitType.Unit)))
    (Pair_fst (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType (Pair_snd
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (PairType
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType) (@PairType.PairValue
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (PairType
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType) (@Stream.rec
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => x'') x'))
    (@PairType.PairValue (CryptolToLean.SAWCoreVectors.Vec 32 Bool) UnitType
    (@Stream.rec (CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (strm' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32
    Bool)) => CryptolToLean.SAWCoreVectors.Vec 32 Bool) (fun (s : Nat ->
    CryptolToLean.SAWCoreVectors.Vec 32 Bool) => s (subNat i 1)) (@PairType.rec
    (Stream (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (_ : PairType (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool))) => Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (fun (x'' : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) (y : Stream
    (CryptolToLean.SAWCoreVectors.Vec 32 Bool)) => y) x')) UnitType.Unit)))))
    #v[bvNat 32 1] i))) (PairType.PairValue (Stream.MkStream lkA_)
    (Stream.MkStream lkB_)))) i_)))

end StreamFibs