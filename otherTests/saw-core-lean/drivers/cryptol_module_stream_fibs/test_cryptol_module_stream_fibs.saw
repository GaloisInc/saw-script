// Phase 5 Slice A.5 end-to-end test — mutual-stream fix translates
// through the new PairStreamCorec recognizer + lowering. Without
// this slice the fix would have hit L-5 and rejected; now it
// translates to a `mkStreamFixPair`-rooted Lean term.

write_lean_cryptol_module
  "StreamFibs.cry"
  "test_cryptol_module_stream_fibs.module.lean" [] [];
