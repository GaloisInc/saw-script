/- Mandatory imports from saw-core-lean -/
import CryptolToLean

open CryptolToLean.SAWCorePrimitives
open CryptolToLean.SAWCoreVectors
open CryptolToLean.SAWCoreBitvectorsProofs
open CryptolToLean.SAWCorePreludeProofs

/- Code generated by saw-core-lean -/

noncomputable def goal : Prop :=
  (p1 : RecordType "x" (Vec 32 Bool) (RecordType "y" (Vec 32 Bool)
  EmptyType)) -> (p2 : RecordType "x" (Vec 32 Bool) (RecordType "y" (Vec 32
  Bool) EmptyType)) -> let p1 := (Pure.pure p1); let p2 := (Pure.pure p2);
  let x__ := (fun (x : Except String (Vec 32 Bool)) (y : Except String
  (RecordType "y" (Vec 32 Bool) EmptyType)) => y);
  let x__' := (fun (_ : RecordType "x" (Vec 32 Bool) (RecordType "y" (Vec 32
  Bool) EmptyType)) => Except String (RecordType "y" (Vec 32 Bool) EmptyType));
  let x__'' := (fun (x : Except String (Vec 32 Bool)) (y : Except String
  (RecordType "y" (Vec 32 Bool) EmptyType)) => x);
  let x__''' := (fun (_ : RecordType "x" (Vec 32 Bool) (RecordType "y" (Vec 32
  Bool) EmptyType)) => Except String (Vec 32 Bool));
  let x__'''' := (fun (x : Except String (Vec 32 Bool)) (y : Except String
  EmptyType) => x); let x__''''' := (fun (_ : RecordType "y" (Vec 32 Bool)
  EmptyType) => Except String (Vec 32 Bool)); let x__'''''' := (Bind.bind p1
  (fun scrut_ => @RecordType.rec "x" (Vec 32 Bool) (RecordType "y" (Vec 32 Bool)
  EmptyType) (fun (_ : RecordType "x" (Vec 32 Bool) (RecordType "y" (Vec 32
  Bool) EmptyType)) => Except String (Vec 32 Bool)) (fun (x : Vec 32
  Bool) (y : RecordType "y" (Vec 32 Bool) EmptyType) => let x : (Except String
  (Vec 32 Bool)) := (Pure.pure x); x) scrut_)); let x__''''''' := (Bind.bind
  (Bind.bind p1 (fun scrut_ => @RecordType.rec "x" (Vec 32 Bool) (RecordType "y"
  (Vec 32 Bool) EmptyType) (fun (_ : RecordType "x" (Vec 32 Bool) (RecordType
  "y" (Vec 32 Bool) EmptyType)) => Except String (RecordType "y" (Vec 32 Bool)
  EmptyType)) (fun (x : Vec 32 Bool) (y : RecordType "y" (Vec 32 Bool)
  EmptyType) => let y : (Except String (RecordType "y" (Vec 32 Bool)
  EmptyType)) := (Pure.pure y); y) scrut_)) (fun scrut_ => @RecordType.rec "y"
  (Vec 32 Bool) EmptyType (fun (_ : RecordType "y" (Vec 32 Bool)
  EmptyType) => Except String (Vec 32 Bool)) (fun (x : Vec 32
  Bool) (y : EmptyType) => let x : (Except String (Vec 32 Bool)) := (Pure.pure
  x); x) scrut_)); let x__'''''''' := (Bind.bind (Bind.bind p2
  (fun scrut_ => @RecordType.rec "x" (Vec 32 Bool) (RecordType "y" (Vec 32 Bool)
  EmptyType) (fun (_ : RecordType "x" (Vec 32 Bool) (RecordType "y" (Vec 32
  Bool) EmptyType)) => Except String (RecordType "y" (Vec 32 Bool) EmptyType))
  (fun (x : Vec 32 Bool) (y : RecordType "y" (Vec 32 Bool)
  EmptyType) => let y : (Except String (RecordType "y" (Vec 32 Bool)
  EmptyType)) := (Pure.pure y); y) scrut_)) (fun scrut_ => @RecordType.rec "y"
  (Vec 32 Bool) EmptyType (fun (_ : RecordType "y" (Vec 32 Bool)
  EmptyType) => Except String (Vec 32 Bool)) (fun (x : Vec 32
  Bool) (y : EmptyType) => let x : (Except String (Vec 32 Bool)) := (Pure.pure
  x); x) scrut_)); let x__''''''''' := (Bind.bind p2
  (fun scrut_ => @RecordType.rec "x" (Vec 32 Bool) (RecordType "y" (Vec 32 Bool)
  EmptyType) (fun (_ : RecordType "x" (Vec 32 Bool) (RecordType "y" (Vec 32
  Bool) EmptyType)) => Except String (Vec 32 Bool)) (fun (x : Vec 32
  Bool) (y : RecordType "y" (Vec 32 Bool) EmptyType) => let x : (Except String
  (Vec 32 Bool)) := (Pure.pure x); x) scrut_)); @Eq (Except String Bool)
  (CryptolToLean.SAWCorePreludeExtra.iteM Bool (Bind.bind (Bind.bind x__''''''
  (fun v_1 => Bind.bind x__''''''''' (fun v_2 => Pure.pure (bvAdd 32 v_1 v_2))))
  (fun v_1' => Bind.bind (Bind.bind x__''''''''' (fun v_1 => Bind.bind x__''''''
  (fun v_2 => Pure.pure (bvAdd 32 v_1 v_2)))) (fun v_2' => Pure.pure (bvEq 32
  v_1' v_2')))) (CryptolToLean.SAWCorePreludeExtra.iteM Bool (Bind.bind
  (Bind.bind x__''''''' (fun v_1 => Bind.bind x__'''''''' (fun v_2 => Pure.pure
  (bvAdd 32 v_1 v_2)))) (fun v_1' => Bind.bind (Bind.bind x__''''''''
  (fun v_1 => Bind.bind x__''''''' (fun v_2 => Pure.pure (bvAdd 32 v_1 v_2))))
  (fun v_2' => Pure.pure (bvEq 32 v_1' v_2')))) (Pure.pure Bool.true) (Pure.pure
  Bool.false)) (Pure.pure Bool.false)) (Pure.pure Bool.true)

theorem goal_holds : goal := by
  sorry