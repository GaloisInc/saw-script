/- Mandatory imports from saw-core-lean -/
import CryptolToLean

open CryptolToLean.SAWCorePrimitives
open CryptolToLean.SAWCoreVectors
open CryptolToLean.SAWCoreBitvectorsProofs
open CryptolToLean.SAWCorePreludeProofs

/- Code generated by saw-core-lean -/

noncomputable def goal : Prop :=
  (x : Vec 16 Bool) -> (y : Vec 16 Bool) -> (z : Vec 16 Bool) ->
  let x := (Pure.pure x); let y := (Pure.pure y); let z := (Pure.pure z); @Eq
  (Except String Bool) (Bind.bind (Bind.bind (Bind.bind x (fun v_1 => Bind.bind
  y (fun v_2 => Pure.pure (bvAdd 16 v_1 v_2)))) (fun v_1' => Bind.bind z
  (fun v_2' => Pure.pure (bvAdd 16 v_1' v_2')))) (fun v_1'' => Bind.bind
  (Bind.bind x (fun v_1' => Bind.bind (Bind.bind y (fun v_1 => Bind.bind z
  (fun v_2 => Pure.pure (bvAdd 16 v_1 v_2)))) (fun v_2' => Pure.pure (bvAdd 16
  v_1' v_2')))) (fun v_2'' => Pure.pure (bvEq 16 v_1'' v_2'')))) (Pure.pure
  Bool.true)

theorem goal_holds : goal := by
  sorry