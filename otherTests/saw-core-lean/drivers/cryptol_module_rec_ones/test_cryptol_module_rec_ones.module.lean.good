/- Mandatory imports from saw-core-lean -/
import CryptolToLean

open CryptolToLean.SAWCorePrimitives
open CryptolToLean.SAWCoreVectors

/- Code generated by saw-core-lean -/
namespace RecOnes
  noncomputable def allTrue : Except String (Stream Bool) :=
    Pure.pure (mkStreamFix Bool (saw_unreachable_default Bool
    "fix lookup out of bounds") (fun lookup_ i_ => let s_ := (@Stream.MkStream
    Bool (fun (i : Nat) => atWithDefault 1 Bool (let scrut_ := (Stream.MkStream
    lookup_); @Stream.rec Bool (fun (strm' : Stream Bool) => Bool)
    (fun (s : Nat -> Bool) => s (subNat i 1)) scrut_) #v[Bool.true] i));
    streamIdx Bool s_ i_))

end RecOnes