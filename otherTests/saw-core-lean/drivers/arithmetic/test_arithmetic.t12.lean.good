/- Mandatory imports from saw-core-lean -/
import CryptolToLean

open CryptolToLean.SAWCorePrimitives
open CryptolToLean.SAWCoreVectors

/- Code generated by saw-core-lean -/

noncomputable def TestArith_ZeroExtend : Except String (Vec 16 Bool) :=
  let x__ := (Num.TCNum 16); Bind.bind (genM 16 Bool
  (fun (i : Nat) => let x__' := (saw_throw_error Bool (Pure.pure
  "at: index out of bounds")); CryptolToLean.SAWCorePreludeExtra.iteM Bool
  (Pure.pure (ltNat i 8)) (atWithDefaultM 8 Bool x__' (Pure.pure (bvNat 8 0)) i)
  (atWithDefaultM 8 Bool x__' (Pure.pure (bvNat 8 5)) (subNat i 8))))
  (fun v_0 => Pure.pure (coerce (Vec 16 Bool) (Vec 16 Bool) (@Eq.rec Num x__
  (fun (y' : Num) (eq' : @Eq Num (Num.TCNum (addNat (subNat 16 8) 8))
  y') => @Eq.{2} Type (Vec 16 Bool) (@Num.rec (fun (num : Num) => Type)
  (fun (n : Nat) => Vec n Bool) (Stream Bool) y')) (@Eq.refl.{2} Type (Vec 16
  Bool)) x__ (by saw_unsafeAssert)) v_0))
