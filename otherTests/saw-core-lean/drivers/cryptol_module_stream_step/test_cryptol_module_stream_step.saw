// Fifth-audit amendment 4 golden (landed with the R3b flip): an
// iterate-shaped stream fix (step = +1, not the identity read) must
// NOT be realized by saw_stream_realize. The recognizer rejects at
// the tail gate ("stream tail is not a Stream.rec read": the +1
// wraps the recursive read) and the emission falls through to the
// generic proof-carrying contract, pinning that the Class S-single
// flip does not over-fire.
write_lean_cryptol_module
  "StreamStep.cry"
  "test_cryptol_module_stream_step.module.lean" [] [];
