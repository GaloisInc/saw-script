module SAWScriptMain where

import Prelude;
import SAWScriptPrelude;

main :: TopLevel #();
main =
  topBind Term #() (read_sbv "examples/add_comm.sbv") (\(t0::Term) ->
  write_aig "add_comm.aig" t0);
