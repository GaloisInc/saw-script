module SAWScriptMain where

import Prelude;
import SAWScriptPrelude;

main :: TopLevel #();
main =
  topBind Term #() (extract_java "Double" "f" java_pure) (\(t0::Term) ->
  print Term t0);
  --write_aig "java_f.aig" t0);
