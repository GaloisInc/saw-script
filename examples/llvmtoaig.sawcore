module SAWScriptMain where

import Prelude;
import SAWScriptPrelude;

main :: TopLevel #();
main =
  topBind Term #() (extract_llvm "examples/double.bc" "f" llvm_pure) (\(t0::Term) ->
  write_aig "llvm_f.aig" t0);
