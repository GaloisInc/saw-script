module SAWScriptMain where

import Prelude;
import SAWScriptPrelude;

main :: TopLevel #();
main =
  topBind Term ProofResult (read_sbv "examples/add_comm.sbv") (\(t0::Term) ->
  topBind ProofResult #() (prove abc t0) (\(r::ProofResult) ->
  print ProofResult r));
