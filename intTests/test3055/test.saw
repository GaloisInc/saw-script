m <- mir_load_module "test.linked-mir.json";

let f_spec = do {
    x <- mir_fresh_var "x" mir_i32;
    y <- mir_fresh_var "y" mir_i32;
    let xs = mir_array_value mir_i32 [mir_term x, mir_term y];
    mir_execute_func [xs];
};

mir_verify m "test::f" [] false f_spec z3;
