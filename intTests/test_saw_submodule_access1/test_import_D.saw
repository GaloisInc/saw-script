import "D.cry";

print (eval_int {{D2::d2}});      // should succeed.
print (eval_int {{D2::D3::d3}});  // should succeed.

// confirming fails of things that shouldn't be in scope:
fails (do {return {{ d2 }};});
fails (do {return {{ D2::d3 }};});
fails (do {return {{ D::D2::d2 }};});

print "done";
