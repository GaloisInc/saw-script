Verify a Cryptol foreign function for Salsa20, implemented using djb's
reference implementation (http://cr.yp.to/snuffle.html), against the
Salsa20 spec in `cryptol-specs`, with the `llvm_ffi_setup` command.
