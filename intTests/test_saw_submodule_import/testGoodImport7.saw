// Test: Verify submodule names work in import lists.

import "D.cry";

import submodule D2 (D3);           // D3 in scope (not its contents)

fails ( do { print {{ d3 }}; } );
print {{D3::d3}};                   // but can access elements w/ D3::
print "done";
