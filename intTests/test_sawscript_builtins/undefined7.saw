// This crashes when m is bound. It will eval until it gets to
// the do-block, and that trips on the undefined.

let m = let x = undefined in do { return (); };
print "oops didn't die";
m;
print "oops still didn't die";
