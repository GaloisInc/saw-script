enable_experimental;

print (get_nopts());
// This will print "misc.saw". test-and-diff.sh doesn't pass further
// arguments so we can't get them.
print (get_opt 0);
//print (get_opt 1);
// Note: need a do-block to trap the failure (see #2424)
fails (do { return (print (get_opt 2)); });
print (get_env "SAW_TEST_ENV");
// Note: need a do-block to trap the failure (see #2424)
fails (do { return (get_env "SAW_MISSING_ENV"); });

// now check show
print (false, show false);
print ("0", show 0);
print ("()", show ());
print ("(1, 2, 3)", show (1, 2, 3));
print ("[1, 2, 3]", show [1, 2, 3]);
print ("\"abc\"", show "abc");
print ("{{ 1 }}", show {{ 1 }});
print ("{| 2 |}", show {| 2 |});
print ("{| [8] |}", show {| [8] |});
let f () = ();
print ("f", show f);
print ("do { return 0; }", show (do { return 0; }));
let r = { x = 3, y = "abc" };
print ("(record)", show r);
print ("\\x -> 3", show (\x -> 3));
print ("return 3", show (return 3));
print ("(TopLevel)", show summarize_verification);
print ("(ProofScript)", show z3);

// we could go on with the 40-odd other kinds of Value,
// but let's not for now
