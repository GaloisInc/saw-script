enable_experimental;

and_true_r <- prove_print z3 {{
   \x ->
   x && True == x
}};

prove_print (do {
   simplify (addsimp and_true_r empty_ss);
   goal_normalize [];
   simplify basic_ss;
   unfolding ["boolEq"];
   trivial;
}) {{
   \y -> y && True == y
}};
