Stress-test E1: bvAdd commutativity over [8].

Cryptol property (from
otherTests/saw-core-lean/test_offline_lean_stress.saw):
    \(x y : [8]) -> x + y == y + x

Emitted goal:
    bvEq 8 (bvAdd 8 x y) (bvAdd 8 y x) = Bool.true

Baseline discharge in proof.lean: `bvAdd_comm 8 x y` + `bvEq_refl 8 _`.
One-line tactic; if awkward or failing, ergonomics need revisiting.

See doc/2026-05-03_stress-test-plan.md for the full sequence.
